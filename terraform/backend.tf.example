# Remote state (optional): copy to backend.tf and create the bucket + DynamoDB table first.
#
# terraform {
#   backend "s3" {
#     bucket         = "your-org-terraform-state"
#     key            = "logging-strategy/terraform.tfstate"
#     region         = "us-east-1"
#     encrypt        = true
#     dynamodb_table = "terraform-locks"
#   }
# }
